# Training-only requirements (in addition to the top-level requirements.txt).
# Inference users do not need any of these.

deepspeed>=0.15.0
peft>=0.13.0
accelerate>=0.34.0
datasets>=2.20.0

# Logging / utilities
tqdm>=4.66.0
PyYAML>=6.0
omegaconf>=2.3.0
